name: build_manually

on:
  issue_comment:
    types: [created, edited, deleted]

    branches:
      - master

jobs:
  build_py_wheel:
    if: "contains(github.event.head_commit.message, '/build')"
    runs-on: ubuntu-latest

    steps:
      - name: say hi
        run: echo hi
